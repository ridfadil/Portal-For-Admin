<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fadil.portaladmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PengaduanFasilitas" />
        <activity android:name=".activity.PengaduanDosen" />
        <activity android:name=".activity.PengaduanOrganisasi" />
        <activity android:name=".activity.PengaduanStatistik" />
        <activity android:name=".activity.Tentang" />
        <activity android:name=".activity.PengaduanAdministrasi"></activity>
    </application>
</manifest>